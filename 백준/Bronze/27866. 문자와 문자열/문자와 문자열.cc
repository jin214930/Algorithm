#include <bits/stdc++.h>
using namespace std;

string s;
int i;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> s >> i;
  cout << s[i - 1];

  return 0;
}
